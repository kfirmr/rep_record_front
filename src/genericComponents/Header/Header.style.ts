import { createStyles } from "../../styles/create-styles";

export const useStyles = () => createStyles({
    container: {
        padding: "5px",
        width: "100vw",
        height: "5vh",
        textAlign: "center",
    }
});
